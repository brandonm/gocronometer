/export_data.csv

# local Cronometer test credentials (never commit)
.cronometer-creds

# local personal diary capture (never commit — contains real health data)
.getdayinfo-sample.txt
testdata/dayinfo-local.txt
.dayinfo-dump.txt

# local infisical project link (never commit)
.infisical.json
